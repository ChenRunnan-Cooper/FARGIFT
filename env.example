# RPC endpoints (choose one or many)
# Primary single RPC (kept for compatibility)
ARBITRUM_SEPOLIA_RPC_URL="https://sepolia-rollup.arbitrum.io/rpc"

# Optional: multiple fallback RPCs (space or comma separated)
# Put your most stable endpoints (Alchemy/Infura) first
# ARBITRUM_SEPOLIA_RPC_URLS="https://arb-sepolia.g.alchemy.com/v2/YOUR_KEY https://sepolia-rollup.arbitrum.io/rpc,https://arbitrum-sepolia.infura.io/v3/YOUR_KEY"

# Private key (hex, 0x-prefixed). DO NOT commit real keys.
PRIVATE_KEY="0xYOUR_PRIVATE_KEY_HEX_64"

# Arbiscan API key (for --verify, optional but recommended)
ARBISCAN_API_KEY="YOUR_ARBISCAN_API_KEY"

# Optional: watcher threshold and poll interval
# THRESH_WEI=2000000000000000   # ~0.002 ETH
# POLL_INTERVAL=20              # seconds 